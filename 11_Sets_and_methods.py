#Sets are unordered data types in Python.Run the print function in a row.
#Each time you will see that the order in the terminal has changed.

this_set = {"Python", "JavaScript", "Java"}
print(this_set)

#We can not reach the item of sets with index method because sets are unordered.

print(this_set[0])